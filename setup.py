from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A complete MLOps implementation for Sentiment Analysis including data versioning (DVC), experiment tracking (MLflow), containerization (Docker), scalable deployment (Kubernetes), and monitoring with Prometheus and Grafana.A complete MLOps implementation for Sentiment Analysis including data versioning (DVC), experiment tracking (MLflow), containerization (Docker), scalable deployment (Kubernetes), and monitoring with Prometheus and Grafana.',
    author='sadat shakeeb',
    license='',
)
